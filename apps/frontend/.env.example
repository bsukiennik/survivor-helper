# Base URL of the backend API (listings + tile proxy). The frontend never
# calls the OSM tile provider directly (AD-3) — every tile request goes
# through this backend.
VITE_API_BASE_URL=http://localhost:3000
